{
  "name": "spinwheels",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @spinwheels/web",
    "build": "npm run build --workspace @spinwheels/web",
    "start": "npm run start --workspace @spinwheels/web",
    "lint": "npm run lint --workspace @spinwheels/web"
  }
}
